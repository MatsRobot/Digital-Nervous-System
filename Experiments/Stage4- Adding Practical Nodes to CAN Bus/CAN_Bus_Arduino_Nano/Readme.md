here is the commented version of the Arduino Nano Node (Node 0x111).

This node acts as a Sonar Sensor station. It reads distance data from a MaxSonar sensor via serial communication, displays the information on a physical 20x4 LCD, and transmits the distance and a heartbeat counter to the PC Dashboard.